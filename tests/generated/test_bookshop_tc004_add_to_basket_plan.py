"""Auto-generated by QAPAL — https://github.com/ahmadsharabati/QAPAL"""
import re

from playwright.sync_api import Page, expect


def test_bookshop_tc004_add_to_basket(page: Page):
    """User adds product to basket"""
    # Step 1: navigate to https://books.toscrape.com/
    page.goto("https://books.toscrape.com/", wait_until="domcontentloaded")

    # Step 2: click (Add to basket)
    page.get_by_role("button", name="Add to basket").click()

    # Assertions
    # Assertion 1: url_contains
    expect(page).to_have_url(re.compile(r".*/.*"))
