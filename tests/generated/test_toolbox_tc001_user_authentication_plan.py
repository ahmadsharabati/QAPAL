"""Auto-generated by QAPAL — https://github.com/ahmadsharabati/QAPAL"""
import re

from playwright.sync_api import Page, expect


def test_toolbox_tc001_user_authentication(page: Page):
    """User can log in successfully"""
    # Step 1: navigate to https://practicesoftwaretesting.com/auth/login
    page.goto("https://practicesoftwaretesting.com/auth/login", wait_until="domcontentloaded")

    # Step 2: fill (email)
    page.get_by_test_id("email").fill("customer2@practicesoftwaretesting.com")

    # Step 3: fill (password)
    page.get_by_test_id("password").fill("welcome01")

    # Step 4: click (login)
    page.get_by_test_id("login").click()

    # Assertions
    # Assertion 1: url_contains
    expect(page).to_have_url(re.compile(r".*/auth/login.*"))
